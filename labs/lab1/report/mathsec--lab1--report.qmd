---
## Author
author:
  name: Магомед Асхабович Мажитов
  degrees: DSc
  orcid: 0000-0002-0877-7063
  email: 1032259381@rudn.ru
  affiliation:
    - name: Российский университет дружбы народов
      country: Российская Федерация
      postal-code: 117198
      city: Москва
      address: ул. Миклухо-Маклая, д. 6

## Title
title: "Лабораторная работы №1"
subtitle: "Шифры простой замены"
license: "CC BY"
---

# Цель работы

Цель работы - изучить и реализовать шифры Цезаря и Атбаш

# Задание

Реализовать шифры Цезаря и Атбаш с помощью языка Julia.

# Теоретическое введение

Julia — высокоуровневый свободный язык программирования с динамической типизацией, созданный для математических вычислений[@julialang]. Эффективен также и для написания программ общего назначения. Синтаксис языка схож с синтаксисом других математических языков, однако имеет некоторые существенные отличия.

Для выполнения заданий была использована официальная документация Julia[@juliadoc].

# Выполнение лабораторной работы

Шифр Цезаря (также известен как шифр сдвига или код Цезаря) — разновидность шифра подстановки, в котором каждый символ в открытом тексте заменяется символом, находящимся на некотором постоянном числе позиций левее или правее него в алфавите.

```julia
function caesar_cipher(text::String, k::Int)
    russAlphabet = "абвгдеёжзийклмнопрстуфхцчшщъыьэюя"
    ciphered_text = []
    for symbol in text
        lower_char = lowercase(string(symbol))[1]
        if occursin(string(lower_char), russAlphabet)
            alpha_chars = collect(russAlphabet)
            index = findfirst(isequal(lower_char), alpha_chars)
            new_index = mod(index + k - 1, 33) + 1
            if isuppercase(symbol)
                push!(ciphered_text, uppercase(alpha_chars[new_index]))
            else
                push!(ciphered_text, alpha_chars[new_index])
            end
        else
            push!(ciphered_text, symbol)
        end
    end
    return join(ciphered_text)
end
```

Атба́ш  — простой шифр подстановки для алфавитного письма. Правило шифрования состоит в замене i-й буквы алфавита буквой с номером n−i+1, где n — число букв в алфавите.

```julia
function atbash_cipher(text::String)
    a = Int('а')
    ya = Int('я')
    ciphered_text = []
    for symbol in text
        asc_symbol = Int(lowercase(symbol)[1])
        if a <= asc_symbol <= ya
            new_char = Char(ya - asc_symbol + a)
            if isuppercase(symbol)
                push!(ciphered_text, uppercase(new_char))
            else
                push!(ciphered_text, new_char)
            end
        else
            push!(ciphered_text, symbol)
        end
    end
    return join(ciphered_text)
end
```
Для вывода результата используем функцию main.

```julia
function main()
    text = "абвгд"
    println(text)
    caesar_cipher_text = caesar_cipher(text, 3)
    println(caesar_cipher_text)
    atbash_cipher_text = atbash_cipher(text)
    println(atbash_cipher_text)
end
```

Результат работы алгоритмов ([рис. @fig-001]).

![Результат](image/res.png){#fig-001 width=70%}

# Выводы

С помощью языка программирования Julia были реализованы шифры Цезаря и Атбаш

# Список литературы{.unnumbered}

::: {#refs}
:::
